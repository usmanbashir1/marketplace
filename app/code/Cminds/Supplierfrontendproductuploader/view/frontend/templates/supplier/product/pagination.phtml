<?php
$currentPage = (int) $block->getRequest()->getParam('p', 1);

$pageSize = 20;

$collection = $block->getCollection();

$totCat = $collection->getSize();
$store_cats = $collection->load();

$pageTot = ceil( $totCat / $pageSize );

?>
<?php if($pageTot > 1) : ?>
<ul class="pagination">
    <?php for ( $i=1; $i<=$pageTot; $i++ ) : ?>
        <?php if ( $i == $currentPage ) : ?>
            <li class="current">
                <a href="<?php echo $block->getUrl('supplier/product/productlist', array('p' => $i)); ?>"><?php echo $i ?></a>
            </li>
        <?php else : ?>
            <li>
                <a href="<?php echo$block->getUrl('supplier/product/productlist', array('p' => $i)); ?>"><?php echo $i ?></a>
            </li>
        <?php endif; ?>
    <?php endfor; ?>
</ul>
<?php endif; ?>
